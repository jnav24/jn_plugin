{% extends "base.twig" %}

{% block content %}
    <div id="page_list">
        <a href="{{ add_page_url }}" class="add-new-h2">Add New Page</a>

        <page-list pages="{{ pages|json_encode() }}" url="{{ plugin_url }}"></page-list>
    </div>
{% endblock %}